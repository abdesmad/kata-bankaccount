--drop table ACCOUNT if exists;
--create table ACCOUNT(ACCOUNT_NUMBER BIGINT , BALANCE DECIMAL(20,3),primary key (ACCOUNT_NUMBER));

--drop table TRANSFER_HISTORY if exists;
--create table TRANSFER_HISTORY (ID bigint generated by default as identity (start with 1),ID_ACCOUNT BIGINT, ID_ACCOUNT_INTERACTION BIGINT, DT_CREATE date default CURRENT_DATE, OPERATION varchar(20),AMOUNT DECIMAL(20,3), BALANCE_BEFORE DECIMAL(20,3), BALANCE_AFTER DECIMAL(20,3),  primary key (ID),FOREIGN KEY (ID_ACCOUNT) REFERENCES ACCOUNT(ACCOUNT_NUMBER));


insert into ACCOUNT (ACCOUNT_NUMBER, BALANCE) values (1, 100.12);
insert into ACCOUNT (ACCOUNT_NUMBER, BALANCE) values (2, 200);

